# Dependencies — installed via `npm ci`, never committed.
node_modules/

# Build output — produced fresh on every CI / Pages deploy.
dist/

# Playwright a11y gate artifacts — generated on every run, never committed.
/test-results/
/playwright-report/
/.pw-browsers/

# Editor / OS noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local logs and dotenvs
*.log
.env
.env.local
